# 第 0 周参考提纲

先做题。这里是希望听到，不是范文。

## 练习

1. 希望听到
- 报错原文里有 HTTP 状态或「模型不存在」一类字。
- 笔记留下的是 stderr，不是「好像不行」。
2. 希望听到
- 指到 `hello_chat.py` 里 `messages[0].content`（约 63–67 行）。
- 知道改的是请求体，不是 Base URL。
3. 希望听到
- Key 和 BASE_URL 成对。
- 同一脚本能换口，说明供应商中立的最小体验。
4. 希望听到
- 预测 **HTTP 401**（钥匙和口不是一家）。
- 不是 200，也不是脚本自己的退出码 2（那是缺 Key）。
- 修复：DeepSeek 的 Key 配 `https://api.deepseek.com/v1`。

## 测验

<details>
<summary>1. DeepSeek Key 配到 api.openai.com，hello_chat 最可能看到什么？</summary>

希望听到：HTTP 401；[`hello_chat.py:82`](../../../code/week0/hello_chat.py) 打印响应体。不是退出码 2（那是空 Key）。

</details>

<details>
<summary>2. 没复制 .env，退出码是？</summary>

希望听到：2。stderr 先写「缺少 .env」，再写 Ollama 提示。[`47:57`](../../../code/week0/hello_chat.py)。

</details>

<details>
<summary>3. 终端出现 [ok] reply= 就证明你会 Agent 了吗？</summary>

希望听到：没有。这是一次 chat completion。文件头写着还不是 Agent。

</details>

<details>
<summary>4. load_dotenv 会覆盖已经 export 的 Key 吗？</summary>

希望听到：不会。`setdefault`。[`16:26`](../../../code/week0/hello_chat.py)。

</details>

<details>
<summary>5. 本周必须有 GPU 吗？</summary>

希望听到：不必须。笔记本发 JSON。模型在云端或 Ollama。

</details>
