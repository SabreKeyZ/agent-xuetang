# 第 2 周参考提纲

## 练习

1. 希望听到
- 第四条 JSON 的 `expect_tool` 是 `search`。
- `expect_contains` 能在 `SEARCH_TABLE` 理赔台那行找到。
2. 希望听到
- `error:division_by_zero`
- 最终答案不是 `inf`、不是 `0`。
3. 希望听到
- `--eval` 退出码 1。
- 至少一行 `[FAIL]`。
4. 希望听到
- 忘字段 → `error:parse` 退出码 1。
- 全角冒号 → `action=calculator`。
5. 希望听到
- CoT 无芯片；ReAct 有字段；工单台政策员是进程内 tool-call。
- 点到 `search_policy` 或 `promo-overrides-sla`。

## 测验

<details>
<summary>1. mix-1 只算对了却没搜，评测过吗？</summary>

希望听到：不过。`expect_tools` 要 search 和 calculator 都在。[`234:236`](../../../code/week2/react_agent.py)

</details>

<details>
<summary>2. 解析器认全角冒号吗？</summary>

希望听到：认。`[：:]`。[`145:148`](../../../code/week2/react_agent.py)

</details>

<details>
<summary>3. 假搜索为什么比真搜索适合作业？</summary>

希望听到：评测稳定。真搜索今天过明天挂。

</details>

<details>
<summary>4. calculator("3/0") 必须是？</summary>

希望听到：`error:division_by_zero`。不要 `eval`。

</details>

<details>
<summary>5. 工单台政策员是在写 ReAct 散文吗？</summary>

希望听到：不是。直接 `search_policy`。第 2 周手写解析是为了看见字段。

</details>
